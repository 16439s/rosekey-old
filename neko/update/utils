color() {
  if [ -t 1 ]; then
    tput setaf "${1:-7}"
  fi
}

say() {
  color 5  # magenta
  printf "( ^-^) < %s\n" "$1"
  color
}

running() {
  color 3  # yellow
  printf "[running] \$ %s\n" "$1"
  color
}

br() {
  printf "\\n"
}
